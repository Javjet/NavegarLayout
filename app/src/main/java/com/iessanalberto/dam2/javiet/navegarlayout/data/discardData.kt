package com.iessanalberto.dam2.javiet.navegarlayout.data

/*

    val disabledItem = 1
    val contextForToast = LocalContext.current.applicationContext
DropdownMenu(
            expanded = expanded,
            onDismissRequest = {
                expanded = false
            }
        ) {
            // adding items
            CientificasLista.forEachIndexed { itemIndex, itemValue ->
                DropdownMenuItem(
                    onClick = {
                        gameViewModel.updateUserGuess(itemValue.nombre)
                        Toast.makeText(contextForToast, itemValue.nombre, Toast.LENGTH_SHORT)
                            .show()
                        expanded = false
                    },
                    enabled = (itemIndex != disabledItem)
                ) {
                    Text(text = itemValue.nombre)
                }
            }
        }*/
